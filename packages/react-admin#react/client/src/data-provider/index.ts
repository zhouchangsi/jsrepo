// type dataProvider = {
//     getList: (resource, params) => Promise<any>, // get a list of records based on sort, filter, and pagination
//     getOne: (resource, params) => Promise<any>, // get a single record by id
//     getMany: (resource, params) => Promise<any>, // get a list of records based on an array of ids
//     getManyReference: (resource, params) => Promise<any>, // get the records referenced to another record, e.g. comments for a post
//     create: (resource, params) => Promise<any>, // create a record
//     update: (resource, params) => Promise<any>, // update a record based on a patch
//     updateMany: (resource, params) => Promise<any>, // update a list of records based on an array of ids and a common patch
//     delete: (resource, params) => Promise<any>, // delete a record by id
//     deleteMany: (resource, params) => Promise<any>, // delete a list of records based on an array of ids
// }



// export const dataProvider: dataProvider = {
//     getList: (resource, params) => new Promise(() => { }), // get a list of records based on sort, filter, and pagination
//     getOne: (resource, params) => new Promise(() => { }), // get a single record by id
//     getMany: (resource, params) => new Promise(() => { }), // get a list of records based on an array of ids
//     getManyReference: (resource, params) => new Promise(() => { }), // get the records referenced to another record, e.g. comments for a post
//     create: (resource, params) => new Promise(() => { }), // create a record
//     update: (resource, params) => new Promise(() => { }), // update a record based on a patch
//     updateMany: (resource, params) => new Promise(() => { }), // update a list of records based on an array of ids and a common patch
//     delete: (resource, params) => new Promise(() => { }), // delete a record by id
//     deleteMany: (resource, params) => new Promise(() => { }), // delete a list of records based on an array of ids
// }


export { }